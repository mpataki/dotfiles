[core]
  excludesfile = ~/.gitignore
  editor = vim
  pager = delta
[color]
  ui = always
[format]
  pretty = %Cblue%h%Creset %Cgreen[%cr]%Creset (%an) %s
[alias]
  b = branch -a -v
  c = commit -m
  a = add
  ap = add -p
  co = checkout
  d = diff
  pl = pull --rebase
  ps = push
  s = status --short
  cp = cherry-pick
  pr = pull-request
  merged = log --date-order --tags --simplify-by-decoration --pretty=format:\"%Cblue%h %Cgreen%<|(20)[%cr]%Creset %C(auto)%<|(41)%d %s\"
  ob = for-each-ref --sort=committerdate refs/heads/ --format='%(HEAD) %(color:yellow)%(refname:short)%(color:reset) - %(color:red)%(objectname:short)%(color:reset) - %(contents:subject) - %(authorname) (%(color:green)%(committerdate:relative)%(color:reset))'
[help]
  autocorrect = 1
[push]
  default = current
[delta]
  navigate = true
  light = false
  side-by-side = false
  line-numbers = true
[interactive]
  diffFilter = delta --color-only
[init]
  defaultBranch = main
[gpg]
  format = ssh
[gpg "ssh"]
  program = "/Applications/1Password.app/Contents/MacOS/op-ssh-sign"
[commit]
  gpgsign = true
[user]
  name = Mat Pataki
  email = matpataki@gmail.com
  signingkey = ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAILH8m/LZhOLEOFtZ2VDsmemChw3A/He/zklcqQecKJ3z
[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
[includeIf "gitdir:~/code/1password/"]
  path = ~/.gitconfig.1password
[includeIf "gitdir:~/code/1password/gitlab"]
  path = ~/.gitconfig.1password-gitlab
[url "ssh://git@gitlab.1password.io:2227"]
        insteadOf = https://gitlab.1password.io
